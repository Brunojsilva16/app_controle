<h1 class="mb-4">Consultar Lançamentos</h1>

<!-- Formulário de Filtro -->
<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-filter me-1"></i>
        Filtros de Pesquisa
    </div>
    <div class="card-body">
        <form action="<?= $_ENV['BASE_URL'] ?>/token/consulta" method="GET">
            <div class="row align-items-end">
                <div class="col-md-3 mb-2">
                    <label for="termo" class="form-label">Paciente ou Token</label>
                    <input type="text" name="termo" id="termo" class="form-control" value="<?= htmlspecialchars($filters['termo'] ?? '') ?>">
                </div>
                <div class="col-md-3 mb-2">
                    <label for="profissional" class="form-label">Profissional</label>
                    <select name="profissional" id="profissional" class="form-select">
                        <option value="">Todos</option>
                        <?php foreach ($profissionais as $prof): ?>
                            <option value="<?= $prof['id_prof'] ?>" <?= ($filters['profissional'] ?? '') == $prof['id_prof'] ? 'selected' : '' ?>>
                                <?= htmlspecialchars($prof['profissional']) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>
                <div class="col-md-2 mb-2">
                    <label for="mes" class="form-label">Mês</label>
                    <select name="mes" id="mes" class="form-select">
                        <option value="">Todos</option>
                        <?php foreach ($meses as $mes): ?>
                             <option value="<?= $mes['valor_li'] ?>" <?= ($filters['mes'] ?? '') == $mes['valor_li'] ? 'selected' : '' ?>>
                                <?= htmlspecialchars($mes['nome_li']) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>
                <div class="col-md-2 mb-2">
                    <label for="ano" class="form-label">Ano</label>
                    <select name="ano" id="ano" class="form-select">
                        <option value="">Todos</option>
                         <?php foreach ($anos as $ano): ?>
                            <option value="<?= $ano['valor_li'] ?>" <?= ($filters['ano'] ?? '') == $ano['valor_li'] ? 'selected' : '' ?>>
                                <?= htmlspecialchars($ano['nome_li']) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>
                <div class="col-md-2 mb-2">
                    <button type="submit" class="btn btn-primary w-100">Buscar</button>
                </div>
            </div>
        </form>
    </div>
</div>


<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Token</th>
                        <th>Paciente</th>
                        <th>Profissional</th>
                        <th>Valor (R$)</th>
                        <th>Tipo Pag.</th>
                        <th>Mês/Ano Ref.</th>
                        <th>Lançado Por</th>
                        <th>Data Lanç.</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($tokens)): ?>
                        <tr>
                            <td colspan="8" class="text-center">Nenhum lançamento encontrado para os filtros selecionados.</td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($tokens as $token): ?>
                            <tr>
                                <td><strong><?= htmlspecialchars($token['token']) ?></strong></td>
                                <td><?= htmlspecialchars($token['paciente']) ?></td>
                                <td><?= htmlspecialchars($token['profissional_nome']) ?></td>
                                <td><?= number_format($token['valorpag'], 2, ',', '.') ?></td>
                                <td><?= htmlspecialchars($token['tipopag']) ?></td>
                                <td><?= htmlspecialchars($token['mes_ref']) ?>/<?= htmlspecialchars($token['ano_ref']) ?></td>
                                <td><?= htmlspecialchars($token['criado_por_nome']) ?></td>
                                <td><?= htmlspecialchars(date('d/m/Y H:i', strtotime($token['data_criacao']))) ?></td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>

