<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $title ?? 'Controle de Token' ?></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome (para ícones) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"/>

    <!-- CSS customizado -->
    <style>
        body {
            overflow-x: hidden;
        }
        #sidebar-wrapper {
            min-height: 100vh;
            margin-left: -15rem;
            transition: margin .25s ease-out;
            background-color: #dc3545; /* Cor baseada na sua imagem */
            color: white;
        }
        #sidebar-wrapper .sidebar-heading {
            padding: 0.875rem 1.25rem;
            font-size: 1.2rem;
            font-weight: bold;
            text-align: center;
        }
        #sidebar-wrapper .list-group-item {
            background-color: #dc3545;
            color: #f8f9fa;
            border: none;
        }
        #sidebar-wrapper .list-group-item:hover,
        #sidebar-wrapper .list-group-item.active {
            background-color: #c82333;
            color: white;
        }
        #sidebar-wrapper .user-profile {
            text-align: center;
            padding: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.2);
        }
        #sidebar-wrapper .user-profile .profile-icon {
            font-size: 50px;
            margin-bottom: 10px;
        }
        #wrapper.toggled #sidebar-wrapper {
            margin-left: 0;
        }
        #page-content-wrapper {
            min-width: 100vw;
        }
        #wrapper.toggled #page-content-wrapper {
            min-width: 0;
            width: 100%;
        }
        .navbar {
            justify-content: start;
        }
        .btn-danger {
            background-color: #c82333;
            border-color: #c82333;
        }
    </style>
</head>
<body>
    <div class="d-flex" id="wrapper">
        <?php if (App\Core\Auth::isLogged()): ?>
            <?php require_once VIEWS_PATH . '/partials/sidebar.phtml'; ?>
        <?php endif; ?>

        <div id="page-content-wrapper">
            <?php if (App\Core\Auth::isLogged()): ?>
                <?php require_once VIEWS_PATH . '/partials/navbar.phtml'; ?>
            <?php endif; ?>

            <div class="container-fluid mt-4">
                <?php require_once VIEWS_PATH . '/partials/messages.phtml'; ?>
                <?= $pageContent ?? '' ?>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Script para o toggle da sidebar -->
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const menuToggle = document.getElementById("menu-toggle");
            if (menuToggle) {
                menuToggle.addEventListener("click", function(e) {
                    e.preventDefault();
                    document.getElementById("wrapper").classList.toggle("toggled");
                });
            }
        });
    </script>
</body>
</html>
